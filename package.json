{
  "name": "task-creation",
  "version": "1.0.0",
  "engines": {
    "node": "14.x || 16.x"
  },
  "scripts": {
    "dev": "cd frontend && npm start & cd api && npm run dev",
    "start": "cd api && npm start",
    "build": "cd frontend && npm run build",
    "install:all": "cd api && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
